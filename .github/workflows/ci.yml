name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    name: Lint GitHub Actions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: raven-actions/actionlint@v2

  verify:
    uses: ./.github/workflows/verify.yml

  publish:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    needs: verify
    uses: edigonzales/hop-plugin-ci/.github/workflows/plugin-publish.yml@main
    with:
      ci-ref: main
      repository-kind: snapshot
      verified-artifact-name: hop-ilivalidator-plugin-canonical
      pom-file: pom.xml
      zip-descriptors: >-
        [{"artifactId":"hop-action-ilivalidator","zipGlob":"assemblies/assemblies-action-ilivalidator/target/hop-action-ilivalidator-*.zip","pluginRoot":"plugins/actions/ilivalidator"},
         {"artifactId":"hop-transform-ilivalidator","zipGlob":"assemblies/assemblies-transform-ilivalidator/target/hop-transform-ilivalidator-*.zip","pluginRoot":"plugins/transforms/ilivalidator"}]
      group-id: ch.so.agi
      hop-version: 2.19.0
    secrets:
      MAVEN_USERNAME: ${{ secrets.INTERLIS_MAVEN_USERNAME }}
      MAVEN_PASSWORD: ${{ secrets.INTERLIS_MAVEN_TOKEN }}
