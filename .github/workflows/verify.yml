name: Verify INTERLIS Validator plugin

on:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    uses: edigonzales/hop-plugin-ci/.github/workflows/plugin-verify.yml@main
    with:
      ci-ref: main
      java-versions: '["21", "25"]'
      runner-os: '["ubuntu-latest", "macos-15", "windows-latest"]'
      canonical-runner-os: ubuntu-latest
      canonical-java-version: '21'
      maven-working-directory: .
      maven-options: -U -B -ntp
      maven-goals: clean verify
      compatibility-goals: clean test
      pom-file: pom.xml
      zip-descriptors: >-
        [{"artifactId":"hop-action-ilivalidator","zipGlob":"assemblies/assemblies-action-ilivalidator/target/hop-action-ilivalidator-*.zip","pluginRoot":"plugins/actions/ilivalidator"},
         {"artifactId":"hop-transform-ilivalidator","zipGlob":"assemblies/assemblies-transform-ilivalidator/target/hop-transform-ilivalidator-*.zip","pluginRoot":"plugins/transforms/ilivalidator"}]
      validation-commands: '["python3 scripts/verify-packages.py", "python3 scripts/run-e2e.py"]'
      artifact-name-prefix: hop-ilivalidator-plugin
      group-id: ch.so.agi
      hop-version: 2.19.0
    secrets: inherit
